@{
  Layout = "_Layout";
}

@using BestRestaurant.Models;

@if (@Model.Count == 0)
{
  <h2>No Cuisines Saved!!!!1!1!1!!!1</h2> 
}

<ul>
@foreach(Cuisine item in Model)
{
  <li>@Html.ActionLink($"{item.CuisineType}", "Details", "Cuisines", new { fish=item.CuisineId})</li>
}
</ul>


<p>@Html.ActionLink("Add A New Cuisine", "Create")</p>